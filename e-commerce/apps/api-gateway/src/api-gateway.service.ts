import { Inject, Injectable } from '@nestjs/common';
import { ECOMMERCE_SERVICE, TRENDYOL_SERVICE } from '@app/common';
import { ClientProxy } from '@nestjs/microservices';

@Injectable()
export class ApiGatewayService {
  constructor(
  ) {
  }

}
