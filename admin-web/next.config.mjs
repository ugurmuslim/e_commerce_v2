/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  i18n: {
    locales: ["tr"], // List of supported locales
    defaultLocale: "tr", // Default locale
  },
};

export default nextConfig;
